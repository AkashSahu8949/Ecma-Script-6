const arr = [{1:1, 2:2},{3:3},{1:1,4:4},{1:1,2:2}];
arr.map(newvalue);
 function newvalue(item){
    arr.forEach(newvalue2);
    function newvalue2(item2){
      
        JSON.stringify(item) === JSON.stringify(item2)
    }
    console.log(JSON.stringify(item))
    return item
}

